---
name: sync-docs
description: Update every doc in docs/ to reflect the latest project state. Use when the user says "sync docs", "update the docs", "refresh project docs", or when a session-start reminder asks for the once-per-conversation doc sync.
---

# Sync Docs

The full skill definition lives with the project's other skills: read `src/skills/sync-docs.md` and follow its procedure exactly. That file is the single source of truth — do not duplicate its content here.
